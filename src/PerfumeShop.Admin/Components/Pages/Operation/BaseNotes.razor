@page "/admin/Operation/BaseNotes"
@using Microsoft.AspNetCore.Components
@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/admin/TechCenter/NoteManagement", true);
    }
}
