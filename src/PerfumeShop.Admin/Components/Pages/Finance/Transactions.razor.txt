<!-- V19财务中心 · 交易流水 -->
@page "/admin/finance/transactions"
@using Microsoft.EntityFrameworkCore
@using PerfumeShop.Data.Models

<PageTitle>交易流水</PageTitle>
<MudText Typo="Typo.h4" Class="mb-4"><i class="fas fa-list-alt" style="color:#00bcd4;"></i> 交易流水</MudText>

@if (_loaded)
{
    <MudTable Items="@_items" Hover="true" Dense="true" Striped="true">
        <HeaderContent><MudTh>ID</MudTh><MudTh>订单ID</MudTh><MudTh>金额</MudTh><MudTh>方式</MudTh><MudTh>状态</MudTh><MudTh>交易ID</MudTh><MudTh>时间</MudTh></HeaderContent>
        <RowTemplate>
            <MudTd>@context.RecordId</MudTd>
            <MudTd>@context.OrderId</MudTd>
            <MudTd Style="font-weight:600;">@context.Amount?.ToString("C2")</MudTd>
            <MudTd>@context.Method</MudTd>
            <MudTd><MudChip T="string" Size="Size.Small" Color="@(context.Status == "Completed" ? Color.Success : Color.Warning)">@context.Status</MudChip></MudTd>
            <MudTd>@context.TransactionId</MudTd>
            <MudTd>@context.PaidAt?.ToString("yyyy-MM-dd HH:mm")</MudTd>
        </RowTemplate>
    </MudTable>
}
else { <MudProgressCircular Color="Color.Primary" Indeterminate="true" Class="my-8" /> }

@code {
    [Inject] private PerfumeShopContext Db { get; set; } = default!;
    private bool _loaded;
    private List<PaymentRecord> _items = new();
    protected override async Task OnInitializedAsync() { _items = await Db.PaymentRecords.AsNoTracking().OrderByDescending(p => p.PaidAt).Take(300).ToListAsync(); _loaded = true; }
}
