<!-- V19财务中心 · 费用分配 -->
@page "/admin/finance/expense-allocation"
@using Microsoft.EntityFrameworkCore
@using PerfumeShop.Data.Models

<PageTitle>费用分配</PageTitle>
<MudText Typo="Typo.h4" Class="mb-4"><i class="fas fa-calculator" style="color:#00bcd4;"></i> 费用分配</MudText>

@if (_loaded)
{
    <MudGrid>
        <MudItem xs="12" sm="4"><MudPaper Class="pa-4 text-center" Elevation="0" Style="background:linear-gradient(135deg,#2d2d44,#1e1e32);border:1px solid rgba(255,255,255,0.06);border-radius:12px;"><div style="font-size:12px;color:#888;">总费用</div><div style="font-size:28px;font-weight:700;color:#F44336;">@_totalExpense.ToString("C2")</div><div style="font-size:11px;color:#666;">@_expenseCount 条记录</div></MudPaper></MudItem>
    </MudGrid>
    <MudTable Items="@_expenses" Hover="true" Dense="true" Striped="true">
        <HeaderContent><MudTh>期间</MudTh><MudTh>成本中心</MudTh><MudTh>金额</MudTh><MudTh>分类</MudTh><MudTh>备注</MudTh><MudTh>时间</MudTh></HeaderContent>
        <RowTemplate>
            <MudTd>@context.Period</MudTd>
            <MudTd>@context.CenterId</MudTd>
            <MudTd Style="color:#F44336;font-weight:600;">@context.Amount?.ToString("C2")</MudTd>
            <MudTd>@context.Category</MudTd>
            <MudTd>@context.Notes</MudTd>
            <MudTd>@context.CreatedAt?.ToString("yyyy-MM-dd")</MudTd>
        </RowTemplate>
    </MudTable>
}
else { <MudProgressCircular Color="Color.Primary" Indeterminate="true" Class="my-8" /> }

@code {
    [Inject] private PerfumeShopContext Db { get; set; } = default!;
    private bool _loaded; private decimal _totalExpense; private int _expenseCount;
    private List<ExpenseRecord> _expenses = new();
    protected override async Task OnInitializedAsync() { _expenses = await Db.ExpenseRecords.AsNoTracking().OrderByDescending(e => e.ExpenseId).Take(200).ToListAsync(); _totalExpense = _expenses.Sum(e => e.Amount ?? 0); _expenseCount = _expenses.Count; _loaded = true; }
}
